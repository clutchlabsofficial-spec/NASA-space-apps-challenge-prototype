// The published standalone page cannot reach Tripo, so no GLB ever exists to
// display. Aliasing the viewer to this stub keeps ~1 MB of 3D engine out of a
// build that could never use it.
export default {}
